# components

Organized React UI components for LIRA feature areas and shared interface elements. Components should be composable, accessible, and small enough to test around a single visual or interaction responsibility.

Feature folders group domain-specific UI, while `shared/` contains elements used by multiple workflows. Components may receive data and callbacks from pages or hooks, but should avoid owning backend integration details.
